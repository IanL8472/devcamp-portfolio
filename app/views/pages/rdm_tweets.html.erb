<div class="inner cover">
  <h1 class="cover-heading cch">Tweets Page</h1>
  
  <div class="row">
    <% @tweets.each do |tweet| %>
      <div class="col-md-6">
        <div class="rdm-tweets">
          <div class="card">
            <%= tweet %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>